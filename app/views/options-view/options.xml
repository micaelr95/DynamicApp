<Page loaded="options">
    <StackLayout orientation="vertical">
        <Button text="Change server" backgroundColor="{{ ButtonColor }}" color="white" onTap="changeServer" />
    </StackLayout>
</Page>
