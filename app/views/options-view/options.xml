<Page loaded="options">
    <StackLayout orientation="vertical">
        <Button text="Change server" backgroundColor="{{ ButtonBackgroundColor }}" color="{{ ButtonTextColor }}" onTap="changeServer" />
    </StackLayout>
</Page>
